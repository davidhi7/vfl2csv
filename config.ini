[Input]
# TSV for tab delimited values or Excel for Excel files
input_format = Excel
input_file_extension = txt

[Output]
csv_output_pattern = {revier}/{versuch}/{parzelle}.csv
metadata_output_pattern = {revier}/{versuch}/{parzelle}_metadata.txt

[Multiprocessing]
enabled = false
sheets_per_core = 32
